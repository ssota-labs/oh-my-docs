---
title: Planning workflow
description: How product intent becomes an implementation plan.
type: guide
---

Dependency direction:

`vision → PRD → story → specification/ADR → implementation plan → code`

Use templates in `docs/templates`. Give every catalog artifact a stable
kind-prefixed ID and register it in the sibling `meta.json`.
